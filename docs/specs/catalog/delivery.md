# Delivery catalog

Forwarding the certified branch, GitHub PR open, allowlisted checks, and Agent-authored PR compose.

| Code | Feature | Capability | Match terms | Surface roots | Spec | Status | Roadmap item | Observation |
|---|---|---|---|---|---|---|---|---|
| DELIVER | Delivery | Forwards the certified branch, opens the GitHub PR, babysits allowlisted checks | pr, forward, checks, allowlist, github | `crates/porch-deliver/src/` | — | Recognized | — | OBS-75c657 |
| FWDAUTH | Durable forward authorization | Records a forward intent before the push and a forward outcome after it, so a crash mid-forward leaves durable evidence instead of a run that looks like it never tried; the forward carries the SHA continuity authorized, and each deliver attempt records at most one forward because an unchanged-HEAD repair hands off to the next attempt. Binding that SHA by equality is blocked on the certify correction-commit question | forward authorization, forward record, head continuity, authorized sha, intent, outcome | `crates/porch-run/src/deliver.rs`, `crates/porch-gate/src/rounds/`, `crates/porch-run/src/lib.rs` | `../2026-09-08-forward-authorization/` | Implemented | ROAD-7 | — |
| RECON | Restart forward reconciliation | Reads the forward record at restart to state what it proves about `origin`, narrowing the undetermined window with the gate repository's own remote-tracking ref rather than a network probe, storing the verdict append-only, and telling the operator the safe remedy without ever asserting a pull request is absent | restart reconciliation, forward conclusion, verdict, indeterminate, tracking ref, interrupted run, reached origin | `crates/porch-gate/src/rounds/`, `crates/porch-gate/src/daemon.rs` | `../2026-09-09-restart-reconciliation/` | Draft | ROAD-8 | — |
| PRCMP | PR compose | Scaffold then park-compose for Agent-authored PR title/body: repo template or default narrative, no self-review theater, hidden attestation | compose, pr body, pr template, scaffold, park compose, attestation | `crates/porch-deliver/src/`, `crates/porch-run/src/deliver.rs`, `crates/porch-gate/src/`, `crates/porch/src/` | `../2026-08-30-pr-compose/` | Implemented | — | — |
