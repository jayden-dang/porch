# Gate catalog

Lifecycle of a pushed ref: admit, hooks, daemon/RPC, disposable-worktree execution, and the git CLI wrapper.

| Code | Feature | Capability | Match terms | Surface roots | Spec | Status | Roadmap item | Observation |
|---|---|---|---|---|---|---|---|---|
| GATE | Gate lifecycle | Accepts a pushed ref and owns run lifecycle and state: admit, hooks, notify, sqlite, daemon/RPC, eject | admit, hook, notify, daemon, eject, custody | `crates/porch-gate/src/`, `crates/porch-gate/tests/` | — | Recognized | — | OBS-95a5d4, OBS-11fc60 |
| RUN | Run execution | Executes one gate run in a disposable worktree: intent, rebase, review, certify, deliver, agent respond | worktree, intent, rebase, certify, respond | `crates/porch-run/src/` | — | Recognized | — | OBS-5df730 |
| GIT | Git wrapper | git CLI wrapper with absolute `--git-dir`; the only place the gate shells out to git | git, force-with-lease, fetch, push, worktree | `crates/porch-git/src/`, `crates/porch-git/tests/` | — | Recognized | — | OBS-5954b0, OBS-fb2db9 |
| ESCAPE | Escape without the daemon | Keeps custody of porch-authored commits whenever a run worktree is dropped — including the rebase case the old ancestry gate silently declined and the superseding-push sweep that never pinned at all — and makes detach independent of the state it escapes: no database open, bare path derived from the home layout, repo id recoverable from the `porch` remote, retryable from any partial state, and a purge that completes or leaves the filesystem untouched. Inspect is LOOK. Refusal on `--purge` is PURGE. | eject, detach, purge, custody, recovery ref, pin, worktree removal, gate state, left behind | `crates/porch-gate/src/custody.rs`, `crates/porch-gate/src/eject.rs`, `crates/porch-gate/src/db.rs`, `crates/porch-gate/src/daemon.rs` | `../2026-09-09-daemon-free-escape/` | Implemented | ROAD-10 | — |
| PURGE | Purge refuses over unforwarded tips | `porch eject --purge` refuses, with no checkout mutation, while this repo has unforwarded **Custody** tips or active **Run**s; `--abandon` is the single explicit override; a refused purge prints a manifest; a completed purge does not report `Purged` when the bare is still there | eject, purge, abandon, refuse, custody tip, recover ref, manifest | `crates/porch-gate/src/eject.rs`, `crates/porch-gate/src/look.rs`, `crates/porch/src/main.rs` | `../2026-09-10-purge-refuses-unforwarded-tips/` | Implemented | ROAD-24 | — |
| DFAULT | Bounded daemon RPC and the daemon-fault suite | Gives every daemon RPC a per-method read/write deadline, so no operator command can wait forever on a daemon that accepts a connection and never answers, and names which of four conditions the daemon is in — ready, unreachable, refusing, not-answering — reported by `porch doctor` and `porch daemon status` without starting a daemon or opening the database. A refused startup is durable in `daemon.refusal.json` and survives retries. Also enforces one daemon per state root: `fs4` reports a contended lock as `Ok(false)`, which the single-instance guard discarded, so two daemons could serve one `$PORCH_HOME` | daemon condition, wedged, not answering, refusing, unreachable, rpc timeout, read deadline, daemon lock, single instance, refusal record, SIGSTOP | `crates/porch-gate/src/condition.rs`, `crates/porch-gate/src/rpc.rs`, `crates/porch-gate/src/daemon.rs`, `crates/porch-gate/src/proc.rs`, `crates/porch/src/doctor.rs`, `crates/porch/tests/m25_daemon_fault.rs` | `../2026-09-09-daemon-fault-suite/` | Implemented | ROAD-11 | — |
| ONEBIN | The installed pair, and what porch says about it | Makes porch coherent about a floor that is a second installed file moving independently of `porch`: refuses to resolve the floor when the running executable's own path no longer names a file, because an install replaced it by rename and the sibling beside it is then the *new* floor under old gate code; gives `doctor` and explicit `--engine quality` one description of the floor through the resolver's own rule, ending a `PATH` requirement the resolver rejects; reports the observed artifact identity the assurance record stores without judging it; and retries a floor spawn the OS reports busy. Documents the v0.2.1 upgrade that installs nothing, and corrects a release smoke gate that asserted a `--version` the floor never had | floor condition, launch replaced, floor identity, sibling, engine quality, install collision, upgrade, ETXTBSY, release smoke | `crates/porch-review/src/floor.rs`, `crates/porch-review/src/setup.rs`, `crates/porch-review/src/lib.rs`, `crates/porch/src/doctor.rs`, `crates/porch/tests/m26_one_binary.rs` | `../2026-09-09-one-binary-install-coherence/` | Implemented | ROAD-12 | — |
| PHASE | Phase-event history | Append-only `phase_events` as the source of truth for a run's phase lifecycle: top-level attempts and ordinals, nested operations, atomic handoffs, crash evidence, derived repair budget, and the audit document's phase slice rendered by `porch audit` | phase event, phase attempt, nested operation, phase handoff, timeline, co-write seam | `crates/porch-gate/src/`, `crates/porch-run/src/`, `crates/porch/src/` | `../2026-09-07-phase-events/` | Implemented | ROAD-5 | — |
